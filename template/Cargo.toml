[package]
name = "{{d.package_name}}"
version = "{{d.version}}"
edition = "{{d.rust_version}}"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
clap = { version = "3.1.18", features = ["derive"] }
